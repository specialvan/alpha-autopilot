[project]
name = "alpha-autopilot"
version = "0.1.0"
description = "Python reimplementation of a heuristic Chinese chess search core"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.135.1",
    "pydantic>=2.12.5",
    "pydantic-settings>=2.13.1",
    "uvicorn>=0.42.0",
]

[tool.ruff]
line-length = 100
