# 小说章节智能推荐引擎开发 PR 需求说明书

## 1. 背景

本项目以爆款拆解为先验，以特征矩阵为核心，以状态推演为中枢，以反馈闭环为进化机制，构建一个面向小说章节推进的智能推荐引擎。

目标是在不依赖海量数据库的前提下，通过少量高质量拆解样本形成可运行的推荐原型，并在后续迭代中不断收敛更稳定的叙事秩序。

## 2. 项目目标

- 构建可解释的章节推荐核心
- 通过特征矩阵表达爆款叙事经验
- 通过状态推演实现多步章节建议
- 通过反馈闭环持续校正评分权重
- 先在 `D:\workspace\alpha-autopilot` 跑通训练与推荐，再迁移整合进 `novel-fusion-autopilot`

## 3. 核心原则

1. 爆款拆解是先验，不是最终答案
2. 特征矩阵是核心，但必须可迭代
3. 状态推演负责“当前写作进程”下的局部最优
4. 反馈闭环负责让系统越用越准
5. 必须保留可解释性和人工可控性

## 4. 系统结构

### 4.1 特征层
从拆解样本中提取如下类特征：

- 冲突推进
- 情绪回报
- 钩子强度
- 叙事连续性
- 角色聚焦
- 伏笔价值
- 节奏适配

### 4.2 状态层
结构化描述当前故事状态：

- 章节位置
- 主线进度
- 支线进度
- 冲突强度
- 情绪温度
- 节奏速度
- 伏笔负载
- 回收压力

### 4.3 推演层
生成候选推进方向并排序：

- 推进冲突
- 强化人物
- 埋设伏笔
- 回收伏笔
- 调整节奏

### 4.4 反馈层
记录推荐结果与真实效果差异，执行权重更新。

## 5. 开发范围

### 当前版本必须完成
- 训练样本读取
- 初版特征矩阵训练
- 章节推荐入口
- 反馈更新接口
- 示例数据集
- 可运行脚本

### 后续版本计划
- 引入更多题材样本
- 支持 JSONL/CSV 数据导入
- 支持多风格权重配置
- 接入 `novel-fusion-autopilot` 的章节生成管线

## 6. 交付物

- Python 原型代码
- 训练脚本 `train.py`
- 推荐脚本 `recommend.py`
- 示例样本 `samples.json`
- PRD 文档
- 项目进度表
- 技术瓶颈复盘文档

## 7. 验收标准

- 能使用示例样本完成矩阵训练
- 能对当前故事状态输出排序后的章节建议
- 能基于反馈进行权重更新
- 输出结果具备解释性
- 结构可继续扩展到 `novel-fusion-autopilot`
