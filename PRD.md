# 小说章节推荐引擎 PRD

## 1. 项目背景

本项目的目标不是先做一个“能跑的 demo”，而是先把章节推荐系统拆成可解释、可验证、可迭代的研究型主链路。

当前 calibration 方向已经明确为三层主链路：

- `rule layer`：负责章节结构约束、叙事硬规则、阶段门槛与安全边界。
- `search layer`：在规则约束下做候选生成、候选排序与局部探索。
- `evaluation loop`：把推荐结果、人工反馈、线上回收数据纳入闭环，持续更新权重与策略。

在这条主链路之外，`quality layer` 不再被描述为独立旁路试验，而是作为“分阶段接入主链路”的质量能力，先做旁路验证，再做可控接入，最后再决定是否进入默认推荐链路。

第三阶段当前权威范围以 `PHASE3_SCOPE_ALIGNMENT_2026_04_24.md` 为准：

- 读者留存目标函数
- 生成控制层
- 以及 recommendation / training / workbench 的受控接入

历史表述“推荐价值自适应进化”仅作为背景，不覆盖当前阶段执行边界。

## 2. 项目目标

- 建立章节推荐的可解释主链路，明确 rule / search / evaluation 的职责边界。
- 让推荐结果能够说明“为什么这样排”“为什么这条候选被选中”。
- 把质量层能力纳入统一术语体系，避免它被误读为独立旁路工程。
- 建立数据库替代的验证口径，做到可比、可回放、可回滚。
- 支持 `v1` 冻结、`v2` 校准、`v3` 增量能力并行推进。

## 3. 核心原则

1. `v1` 冻结，保持当前可运行、可回放的稳定基线，不再追求新能力扩展。
2. `v2` 负责校准，重点是 rule/search/evaluation 闭环、API contract、测试分层和质量口径统一。
3. `v3` 负责增量能力，不是旁路实验，而是面向主链路的可插拔质量层、留存目标函数与生成控制层增强。
4. 数据库替代先验证、后替换，先证明“等价或更优”，再讨论主链路切换。
5. 所有新能力必须保留可解释性和人工可控性。

## 4. 系统结构

### 4.1 Rule Layer

rule layer 负责：

- 章节阶段规则
- 主线 / 支线约束
- 冲突强度阈值
- 情绪温度上下限
- 节奏与伏笔约束
- 质量与安全边界规则

### 4.2 Search Layer

search layer 负责：

- 在 rule layer 约束下生成候选
- 对候选进行初排、重排与局部探索
- 输出 Top-K 候选及其解释
- 支持多策略搜索，但必须受规则约束

### 4.3 Evaluation Loop

evaluation loop 负责：

- 记录推荐是否被采纳
- 记录人工修正、读者反馈、审稿结论
- 更新权重、阈值与排序策略
- 形成“推荐 -> 使用 -> 反馈 -> 更新”的闭环

### 4.4 Quality Layer

quality layer 的阶段口径如下：

- Stage 1：旁路质量分析，只产出诊断结果，不影响主推荐。
- Stage 2：软接入主链路，质量分作为排序参考或风险提示。
- Stage 3：成为主链路的一部分，参与默认决策，但保留可降级开关。

## 5. 数据库替代验证口径

数据库替代不是直接上线切换，而是分三步验证：

1. 等价性验证：新方案与旧数据库在核心样本上输出一致或可接受偏差。
2. 风险边界验证：确认异常输入、空值、重复写入、回滚场景下行为可控。
3. 切换验证：在受控流量或受控任务上验证稳定性，再决定是否扩大范围。

风险边界包括：

- 不能破坏主线推荐可回放性
- 不能让训练日志、版本快照、反馈记录失真
- 不能让质量层结果遮蔽主链路错误

## 6. v1 / v2 / v3 并行策略

- `v1`：冻结现有主干能力，保证稳定回归。
- `v2`：校准推荐链路、API、测试和数据契约。
- `v3`：增量引入 taxonomy、style DNA、checkpoint、admission、quality layer 接入能力，并追加留存目标函数与生成控制层。

三者并行但职责不同：

- `v1` 只负责稳定基线。
- `v2` 只负责校准与收敛。
- `v3` 只负责主链路增量，不作为独立侧项目叙述。

## 7. 开发范围

### 当前必须完成

- 训练样本读取
- 初版特征矩阵训练
- 章节推荐入口
- 反馈更新接口
- 示例数据集
- 可运行脚本

### 后续版本计划

- 引入更多题材样本
- 支持 JSONL / CSV 导入
- 支持多风格权重配置
- 接入质量层主链路阶段口径
- 接入数据库替代验证接口

## 8. 交付物

- Python 原型代码
- 训练脚本 `train.py`
- 推荐脚本 `recommend.py`
- 示例样本 `samples.json`
- PRD 文档
- 项目进度表
- 技术瓶颈与验证口径文档

## 9. 验收标准

- 能用示例样本完成 rule / search / evaluation 闭环。
- 能对当前故事状态输出可解释的章节建议。
- 能记录反馈并更新权重。
- 能明确说明 quality layer 当前处于哪一个接入阶段。
- 能说明留存目标函数如何影响主决策输出，而非仅做旁路展示。
- 能说明数据库替代验证的范围、风险边界和回退方式。
