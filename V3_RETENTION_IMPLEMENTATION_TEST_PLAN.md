# V3 留存目标函数与生成控制层测试计划

## 1. 测试目标

验证 `V3` 的留存目标函数、生成控制层和 pipeline 接入是否符合预期，并且不会破坏 `v1/v2` 的稳定路径。

## 2. 测试范围

### 2.1 留存目标函数

验证：

- 是否能生成稳定的留存目标函数对象
- 是否包含正确的顶层目标描述
- 是否包含必要的 guardrails
- 是否与现有结构化能力兼容

### 2.2 留存指标

验证：

- `chapter_attraction_score` 是否在合理区间内
- `continue_reading_intent` 是否稳定可计算
- `template_risk` 是否能区分偏模板化内容
- 不同章节结构下的指标是否有明显差异

### 2.3 生成控制层

验证：

- 是否能根据留存指标输出控制建议
- 是否能给出情绪、节奏、悬念、冲突、钩子等建议
- 是否能识别 anti-pattern 风险
- 是否能对低吸引力内容给出修正策略

### 2.4 pipeline 接入

验证：

- `decompose_chapter_text()` 是否仍能输出原有拆解信息
- 是否正确写入留存信号、吸引力分数、控制建议
- `workbench_context` 是否包含留存与控制层信息
- `admission` 逻辑是否未被破坏

### 2.5 基线兼容性

验证：

- `v1/v2` 默认路径不受影响
- 现有章节拆解测试仍可通过
- 现有工作台展示接口不发生阻断
- 训练/导入链路仍保持可用

## 3. 推荐测试类型

### 3.1 单元测试

适合验证：

- `RetentionTargetFunction`
- `RetentionMetrics`
- `build_generation_control_plan()`
- 标签到决策映射

### 3.2 集成测试

适合验证：

- `decompose_chapter_text()` 的完整输出
- 留存指标写回 `workbench_context`
- 生成控制层与章节拆解结果联动

### 3.3 回归测试

适合验证：

- 旧的 `v2` / 质量层行为没有被破坏
- 旧 checkpoint / admission 逻辑仍然成立
- 旧 workbench context 字段仍然兼容

### 3.4 验收测试

适合验证：

- 输出是否更偏向“吸引继续阅读”而不是“套模板”
- 是否能明显看到钩子、节奏、冲突、悬念的控制建议
- 是否能在不同题材和阶段下输出不同策略

## 4. 建议测试用例

### 4.1 章节拆解样本

准备至少三类样本：

- 高冲突、高钩子章节
- 中性过渡章节
- 偏模板化、弱吸引力章节

### 4.2 断言点

每个样本至少检查：

- primary function
- retention score
- continue reading intent
- template risk
- control suggestions
- workbench context retention block

### 4.3 边界样本

- 空文本
- 单段文本
- 只有对话的文本
- 只有叙述没有冲突的文本
- 长文本但没有明显钩子的文本

## 5. 验收门槛

### 功能门槛

- 留存目标函数能稳定创建
- 留存指标能稳定计算
- 生成控制层能稳定输出建议
- pipeline 能把结果写回 workbench context

### 稳定性门槛

- 现有章节拆解不失效
- 现有 admission 逻辑不失效
- 旧的基线路径不被覆盖

### 文档门槛

- 路线图和任务文档已同步
- Codex 入口已同步
- 设计说明与实现说明一致

## 6. 推荐执行顺序

1. 先跑单元测试
2. 再跑章节拆解集成测试
3. 再跑回归测试
4. 最后跑验收测试

## 7. 一句话总结

> 这批测试要证明：`V3` 能把“留住读者”落成可计算、可控制、可回写的能力，同时不破坏 `v1/v2` 的稳定路径。