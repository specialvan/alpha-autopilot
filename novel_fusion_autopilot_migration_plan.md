# novel-fusion-autopilot 迁移说明

## 1. 迁移目标

将 `alpha-autopilot` 的小说章节推荐原型迁移为 `novel-fusion-autopilot` 中的一个可插拔推荐子系统。

## 2. 迁移原则

1. 先保留接口，不强求一次重构完成
2. 先保持原型可运行，再做工程整合
3. 先统一数据契约，再统一存储与服务
4. 先接入推演结果，再接入生成流程

## 3. 迁移步骤

### 第一步：契约冻结
- 冻结 `StoryState`、`CharacterState`、`NarrativeCandidate`、`RecommendationResult` 数据结构
- 冻结版本快照与训练日志格式

### 第二步：服务抽象
- 将训练、推荐、反馈、版本管理拆成服务层
- 暴露标准化接口供 `novel-fusion-autopilot` 调用

### 第三步：输入适配
- 从小说大纲、章节摘要、角色卡、世界观卡中构建状态对象

### 第四步：输出适配
- 将推荐结果映射为章节建议、场景建议、角色推进建议

### 第五步：闭环接入
- 接收真实写作结果或人工评审反馈
- 更新矩阵并保存版本

## 4. 迁移风险

- 字段定义不统一导致接入成本上升
- 推荐动作空间不足导致策略单一
- 反馈质量不稳定导致权重震荡
- 过早耦合生成器会增加调试成本

## 5. 推荐路径

建议采用“先原型后集成”的方式：

- `alpha-autopilot` 继续作为研究原型和规则验证场
- `novel-fusion-autopilot` 逐步接收接口和服务
- 评审通过后再合并成正式模块

## 6. 交付边界

本次迁移说明仅包含接口草案与迁移策略，不包含最终实现代码。最终实现应在 `novel-fusion-autopilot` 中依据实际工程结构落地。
