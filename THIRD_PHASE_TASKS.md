# 第三阶段：读者留存目标函数与生成控制层任务清单

## 1. 阶段定位

第三阶段属于 `Increment` 层的核心任务之一。

第三阶段的主目标是：

> 让系统不只生成结构正确的小说内容，而是生成更能留住读者继续阅读的内容。

这一阶段不是替代 `v1/v2`，而是在其基础上追加新的顶层目标函数和生成控制层。

范围锚点：

- 第三阶段权威范围以 `PHASE3_SCOPE_ALIGNMENT_2026_04_24.md` 为准。
- 历史表述“推荐价值自适应进化”仅作为背景，不覆盖当前执行范围。

## 2. 是否与前两阶段冲突

### 结论
**不冲突。**

### 原因
- 第一阶段已经解决“能不能跑通”的问题。
- 第二阶段负责“能不能更稳定、更可用”的问题。
- 第三阶段则在前两者基础上解决“能不能更留人、更抓人”的问题。

三者是递进关系，不是并行抢目标。

## 3. 第三阶段主目标拆解

### 3.1 留存目标函数

#### 目标
把推荐/生成从“结构正确”升级为“留人优先”。

#### 任务
- 定义读者留存核心指标
- 定义章节吸引力代理指标
- 定义继续阅读意愿代理指标
- 定义不同题材的目标函数权重

#### 产出
- `RetentionTargetFunction`
- `RetentionProxyMetrics`
- `ChapterAttractionMetrics`

### 3.2 生成控制层

#### 目标
让情绪、节奏、爽点、悬念、冲突等标签真正参与生成决策。

#### 任务
- 定义生成前控制输入
- 定义控制层输出建议
- 将标签影响接入章节建议与排序
- 支持不同题材/阶段动态调权

#### 产出
- `GenerationControlPolicy`
- `RetentionAwareGenerationPlan`
- `TagDecisionMapping`

### 3.3 标签重定义

#### 目标
把原本偏描述性的标签变成可影响决策的控制变量。

#### 任务
- 重定义情绪、节奏、爽点、悬念、冲突、钩子标签
- 将标签从展示字段升级为决策输入
- 定义标签与输出建议之间的映射

#### 产出
- `TagDecisionSchema`
- `TagWeightPolicy`
- `TagDriftRules`

### 3.4 评估闭环

#### 目标
让系统知道什么内容更能留住读者。

#### 任务
- 定义留存代理指标
- 定义章节吸引力评分
- 定义模板化风险评分
- 定义低吸引力修正建议

#### 产出
- `RetentionEvaluationReport`
- `AttractionScoreboard`
- `TemplateRiskDetector`

### 3.5 反馈闭环

#### 目标
把实际效果回写到目标函数和控制层。

#### 任务
- 记录留存相关反馈
- 回写标签权重
- 回写生成策略
- 回写题材/阶段偏好

#### 产出
- `RetentionFeedbackLoop`
- `ControlWeightUpdater`
- `StrategyRevisionLog`

## 4. 推荐开发顺序

### 第一批
- 定义留存目标函数
- 增加留存代理指标
- 保留现有标签体系，但开始重定义其作用

### 第二批
- 实现生成控制层
- 建立标签到决策的映射
- 建立章节吸引力评分

### 第三批
- 实现反馈回写
- 实现动态调权
- 实现版本对比与回滚依据

## 5. 验收标准

- 系统能够把“留人”作为顶层目标
- 系统能够根据标签给出控制建议
- 系统能够避免明显模板化输出
- 系统能够在不同题材/阶段下动态调权
- 系统能够把反馈回写到策略层

## 6. 与第二阶段的边界

### 第二阶段负责
- 样本扩充
- schema 冻结
- 工程稳定性
- 评估体系基础版
- 章节生成链路基础版

### 第三阶段负责
- 留存目标函数
- 生成控制层
- 标签重定义
- 留存导向评估
- 反馈驱动的策略更新

## 7. 一句话总结

第三阶段不是继续做“能生成”的辅助，而是做“更能留人”的生成控制与目标函数升级。

## 8. 阶段收尾状态（2026-04-24）

### 已收口
- 目标函数与控制层定义、代码接入与回归测试
- decision contract 语义拆分（`blocked` / `prerequisite_missing`）
- training projection 接入与 workbench 质量增强展示
- 分层测试脚本与测试用例索引补齐
- 顶层文档口径统一

### 转后续维护
- 真实章节上下文 API 替换 `mapped_chapter`
- 更细粒度留存反馈闭环（动态调权与长期追踪）
- 跨题材和长序列压力场景扩展验证
