# alpha-autopilot 路线图

## 1. 项目定位

`alpha-autopilot` 是一个面向小说章节推荐与写作辅助的研究型原型项目。当前方向已经从“单点原型”调整为“可验证主链路 + 分阶段质量层接入 + 数据库替代验证”。

统一主链路为：

- `rule layer`
- `search layer`
- `evaluation loop`

质量层作为分阶段能力接入主链路，不再按独立旁路项目表述。

## 2. 总体阶段

### 2.1 `v1` 冻结

目标：保留稳定基线，不再追求功能扩展。

内容：

- 现有推荐与训练能力冻结
- 现有脚本、样本和回归测试冻结
- 仅修复阻断性问题

### 2.2 `v2` 校准

目标：把主链路校准成可解释、可回放、可验证的系统。

重点：

- rule / search / evaluation 闭环
- API contract 统一
- 测试分层
- 质量层接入阶段口径
- 数据库替代验证口径

### 2.3 `v3` 增量

目标：在不破坏主链路的前提下，追加读者留存目标函数与生成控制层，并完成受控接入。

重点：

- taxonomy / schema
- style DNA
- checkpoint / admission
- quality layer 接入
- 留存目标函数
- 生成控制层
- corpus-level QC

## 3. 当前进度

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| 叙事状态建模 | 已完成 | 已具备 `StoryState` / `CharacterState` / 规则字段 |
| 特征矩阵原型 | 已完成 | 已具备训练、版本、日志、回放基础 |
| 主链路规则层 | 已完成 | 已形成推荐前置约束 |
| 主链路搜索层 | 已完成 | 已形成候选生成与排序 |
| 主链路评估闭环 | 已完成 | 已具备反馈更新接口 |
| `v2` 校准工作台 | 进行中 | 需要统一契约与质量层口径 |
| `v3` 留存与控制层增量能力 | 进行中 | 需要形成“目标函数 -> 主决策链路”的真实驱动闭环 |
| 数据库替代验证 | 进行中 | 需要明确等价性、风险边界、回退方式 |

## 4. 风险与边界

### 4.1 质量层仍可能被误读为旁路

如果文档继续把 `v3` 写成独立实验，会导致：

- 主链路与质量层长期双轨
- 质量能力难以反哺推荐
- 后续维护成本增加

第三阶段统一口径以 `PHASE3_SCOPE_ALIGNMENT_2026_04_24.md` 为准，历史表述“推荐价值自适应进化”仅作背景，不作为执行范围。

### 4.2 数据库替代不能直接切换

替代必须先验证：

- 输出等价性
- 异常输入处理
- 回滚可用性
- 回放一致性

### 4.3 测试需要重新分层

建议分为：

- 核心推荐回归
- 质量层回归
- API contract tests
- 前端页面 tests
- 导入脚本 sanity tests

## 5. 下一步计划

1. 把 `v3` 留存目标函数与生成控制层接入主链路，而不是继续作为独立叙述。
2. 统一 `rule layer / search layer / evaluation loop` 的术语。
3. 把 `MASTER_ROADMAP/PRD/ROADMAP/THIRD_PHASE_*` 的第三阶段口径统一到同一权威范围。
4. 完成数据库替代验证口径和风险边界说明。
5. 锁定 `v1`，用 `v2` 做校准，用 `v3` 做留存驱动增量。

## 6. 里程碑

### M1

- 主链路术语统一
- 现有能力完成冻结
- 回归测试可稳定通过

### M2

- quality layer 旁路诊断接入
- 搜索与评估闭环完成对齐，并输出留存驱动证据
- 数据库替代完成第一轮验证

### M3

- quality layer 软接入主链路
- `v3` 留存目标函数与生成控制层纳入默认流程
- 形成可审计的版本与回放体系

## 7. 结论

路线图的核心不是继续堆功能，而是把系统从“能跑”推进到“能解释、能收敛、能回放、能迁移”。`v1`、`v2`、`v3` 需要并行，但职责必须分开，尤其不能再把 `v3` 说成与主链路无关的侧实验。
