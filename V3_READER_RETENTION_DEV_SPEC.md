# V3 读者留存目标函数与生成控制层开发规范

## 1. 文档目的

本文档用于把 `V3` 的需求从方向说明收敛为可执行的开发规范，供 Codex 评审、实现与回归验证。

## 2. 背景

`v1/v2` 已完成稳定闭环、工作台收口与基础结构化写作辅助，但最初立项时没有把“留住读者”抽象成顶层目标函数。

因此当前需要在 `V3` 中追加：

- 读者留存目标函数
- 生成控制层
- 标签到决策的映射
- 留存导向评估与反馈闭环

目标不是推翻现有结构能力，而是让结构能力服务于更高层的留存目标。

## 3. 核心问题定义

当前系统的主要风险是：

- 结构可能正确，但不一定抓人
- 标签可能很多，但只停留在描述层
- 生成结果可能像模板填充，而不是持续吸引读者
- 项目目标可能偏向“写得完整”，而不是“让人继续读”

`V3` 的任务是把“继续读下去”作为显式目标函数嵌入系统。

## 4. 目标函数定义

### 4.1 顶层目标

> 在满足结构正确、风格一致、信息可控的前提下，最大化章节对读者的持续吸引力与继续阅读意愿。

### 4.2 可控子维度

至少包括：

- 情绪推进
- 节奏推进
- 爽点密度
- 悬念保持
- 冲突增长
- 角色吸引
- 章节钩子
- 信息释放节奏

### 4.3 优先级

生成决策应优先满足：

1. 留存优先
2. 结构约束
3. 风格一致
4. 事实/世界观一致
5. 局部模板约束

## 5. 生成控制层定义

### 5.1 职责

生成控制层负责把顶层目标函数转化为具体生成策略。

它应决定：

- 是否加压
- 是否缓冲
- 是否延迟揭示
- 是否提前给钩子
- 是否增加爽点
- 是否强化冲突
- 是否压缩解释

### 5.2 输入

生成控制层至少应接收：

- 章节上下文
- 角色状态
- 事件状态
- 风格状态
- 读者留存目标
- 当前章节阶段
- 前后章节关系
- 风险约束

### 5.3 输出

生成控制层至少应输出：

- 本章目标倾向
- 情绪曲线建议
- 节奏调整建议
- 爽点布局建议
- 悬念保留建议
- 冲突推进建议
- 章节结尾钩子建议

## 6. 标签体系重定义

### 6.1 原则

现有标签不能只作为展示字段，必须升级为可参与决策的控制变量。

### 6.2 标签示例

- 情绪强度 -> 决定当前段落是推进、缓冲还是爆发
- 节奏密度 -> 决定信息释放速度
- 爽点间隔 -> 决定关键回报的布局
- 悬念长度 -> 决定信息延迟程度
- 冲突增幅 -> 决定是否继续加压
- 章节钩子 -> 决定结尾是否留尾巴

### 6.3 使用要求

- 标签必须影响排序或生成
- 标签必须可验证
- 标签必须可回写评估体系
- 标签必须能在不同题材/阶段动态调权

## 7. 评估与反馈

### 7.1 评估维度

至少增加：

- 留存代理指标
- 章节吸引力评分
- 继续阅读意愿代理指标
- 模板化风险评分

### 7.2 反馈闭环

需要支持：

- 留存相关反馈记录
- 标签权重回写
- 策略回写
- 低吸引力片段修正建议

## 8. 与现有阶段的关系

### `v1/v2`

- 保持稳定闭环
- 保持工作台与现有辅助能力
- 维持现有可运行路径

### `V3`

- 追加留存目标函数
- 追加生成控制层
- 让标签参与生成决策
- 建立留存导向评估与反馈闭环

`V3` 是增量层，不是替代层。

## 9. 实现要求

### 9.1 必须满足

- 不破坏 `v1/v2` 基线
- 新控制层可回退、可关闭、可降级
- 结构约束仍然存在，不得失控
- 标签重定义必须可解释

### 9.2 不允许

- 不允许把项目改成纯模板生成器
- 不允许删除结构约束
- 不允许把实验方案写成默认主线
- 不允许让新目标函数误伤现有稳定路径

## 10. 测试要求

建议补充：

- 留存目标函数单测
- 标签到决策映射单测
- 生成控制层集成测试
- `v1/v2` 回归测试
- 章节吸引力相关验收测试

## 11. 文档联动要求

本规范落地后，应同步检查并更新：

- `THIRD_PHASE_ROADMAP.md`
- `THIRD_PHASE_TASKS.md`
- `PROJECT_STATUS.md`
- `claude_review_package/CODEx_REVIEW_DEV_TASK.md`
- `claude_review_package/README_FOR_CODEX.md`

## 12. 验收标准

- 能把“留住读者”作为显式目标
- 能让标签真正影响生成控制
- 能输出可解释的章节建议
- 能避免明显模板化输出
- 能在不破坏基线的前提下逐步接入

## 13. 一句话总结

> `V3` 的核心不是“更会套结构”，而是把“留住读者”变成顶层目标函数，并将情绪、节奏、爽点等标签升级为真正参与生成决策的控制变量。